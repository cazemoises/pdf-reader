// Package ports will declare the interfaces the backend core depends on
// (repositories, the extractor client, notifications, etc), implemented
// later by internal/adapters/*. Left empty on purpose: the orchestrator's
// Dev agent builds this out task by task, driven by data/vision.md and
// data/backlog.json.
package ports
